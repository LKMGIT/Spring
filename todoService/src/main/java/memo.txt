RedirectAttributes / 리다이렉션
PostRedirectGet 패턴
POST 방식으로 어떤 처리를 하고
Redirect 를 해서 GET 방식으로
특정한 페이지로 이동
스프링MVC에서는 PRG 처리방법
RedirectAttributes 타입을 이용하여 처리함
- addAttribute(키,값)
- addFlashAttribute(키,값)
리턴타입
- void
  컨트롤러의 @RequestMapping @GetMapping 메소드에서 선언된 값을 그대로 뷰의 이름으로 사용된다.
- String
  상황에 따라서 다른 화면을 보여줄때 사용된다.
  문자열의 경우 특별한 사용할 수 있다.
  "redirect:/ex06"
  "forward: 브라우저의 URL은 고정되고 내부적으로 다른 URL 로 처리하는 경우


  Mybatis 작업 순서